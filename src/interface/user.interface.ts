export type userTokenData = {
    username: string,
    email: string,
    id: number
}

export type UserFormalData = {
    email: string,
    first_name: string,
    last_name: string,
    username: string
}